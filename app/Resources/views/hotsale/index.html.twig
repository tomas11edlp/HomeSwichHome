{% extends '@Pg/Paginator/tableinline.html.twig' %}

{% block title %}
    <h2>Hot Sales
        <a class="btn btn-outline-success ml-2" href="{{ path('propiedad_new') }}"> Nuevo </a>
    </h2>
    <br>
{% endblock %}

{% block paginator_table_header %}
    {# <th></th> #}
    <th scope="col" class=""></th>
    <th scope="col" class="">Propiedad</th>
    <th scope="col" class="">Semana</th>
    <th scope="col" class="">Precio</th>
    <th scope="col"></th>
{% endblock %}

{% block paginator_table_data %}
    <td class="align-middle">  
        <img src="{{entity.propiedad.foto|default('https://proxy.duckduckgo.com/iu/?u=http%3A%2F%2Fmoorestown-mall.com%2Fnoimage.gif&f=1')}}" width="50%" border="0" style="max-width:100px; max-heigth:100px;">  
    </td>

    {% if entity.reserva is not null %}
        <td class="align-middle">{{ entity.reserva.propiedad }}</td>
        <td class="align-middle">$ {{ entity.reserva.semana }}</td>
    {% else %}
        <td class="align-middle">{{ entity.subasta.propiedad }}</td>
        <td class="align-middle">$ {{ entity.subasta.semanaReserva }}</td>
    {% endif %}
    
    <td class="align-middle">$ {{ entity.precio }}</td>

    <td class="align-middle">
        <a class="ml-4 text-info" href="{{path('propiedad_show', {'id':entity.id}) }}" title="Ver"><i class="fas fa-eye"></i></a>
        <a class="ml-4 text-info" href="{{path('propiedad_edit', {'id':entity.id}) }}" title="Modificar"><i class="fas fa-edit"></i></a>
        <a class="ml-4 text-danger" href="{{path('propiedad_delete', {'id':entity.id}) }}" title="Eliminar"><i class="fas fa-trash"></i></a>
    </td>
{% endblock %}