{% extends 'base.html.twig' %}

{% block javascripts %}
    {{parent()}}
{% endblock %}


{% block body %}

{{ form_start(edit_form) }}

<div class="container-fluid">
    <div class="row justify-content-center mb-3 text-center">
        <div class="col">
            <h3>
                Nuevo medio de pago
            </h3>
        </div>
    </div>

    <div class="row justify-content-center mb-3 text-center">
        <div class="col-3">
            {{ form_row(edit_form.titular) }}
        </div>
    </div>
    <div class="row justify-content-center mb-3 text-center">
        <div class="col-3">
            {{ form_row(edit_form.marca) }}
        </div>
    </div>
    <div class="row justify-content-center mb-3 text-center">
        <div class="col-3">
            {{ form_row(edit_form.numero) }}
        </div>
    </div>
    <div class="row justify-content-center mb-3 text-center">
        <div class="col-3">
            {{ form_row(edit_form.codigo) }}
        </div>
    </div>
    <div class="row justify-content-center mb-3 text-center">
        <div class="col-3">
            {{ form_row(edit_form.fechaVto) }}
        </div>
    </div>    
        

    </div>
    <div class="row justify-content-center mt-5">
        <div class="col-2 text-center">
            <div class="row justify-content-around">
                <a class="btn btn-secondary" href="{{path('perfil_cliente_show', {id : usuario.id})}}">Cancelar</a>
                <button class="btn btn-success" type='submit'>Confirmar</button>
            </div>
        </div>
    </div>
</div>
{{ form_end(edit_form) }}


{% endblock %}
